import { InvisibleDirective } from './invisible.directive';

describe('InvisibleDirective', () => {
  it('should create an instance', () => {
    const directive = new InvisibleDirective();
    expect(directive).toBeTruthy();
  });
});
